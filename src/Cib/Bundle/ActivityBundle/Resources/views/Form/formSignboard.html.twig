{% form_theme form 'form_table_layout.html.twig' %}
<br>
{% include 'CibCoreBundle:Core:displayFormErrors.html.twig' %}
<div class="container">
    <div class="row">
        <div class="col-md-offset-4 col-xs-offset-4 col-xs-3 col-md-3">
            <table class="table table-responsive">
                <tr>
                    <td>Enseigne</td>
                    <td>{{ form_widget(form.signboardName) }}</td>
                </tr>
                <tr>
                    <td>Numéro</td>
                    <td>{{ form_widget(form.signboardNumber) }}</td>
                </tr>
            </table>
            {{ form_rest(form) }}
            <button type="submit" value="valider" name="valider" class="btn btn-primary">Valider</button>
        </div>
    </div>
</div>